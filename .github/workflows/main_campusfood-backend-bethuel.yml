name: Build and deploy Node.js backend to Azure Web App - campusfood-backend-bethuel

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js version
        uses: actions/setup-node@v4
        with:
          node-version: '24.x'
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json

      - name: Install backend dependencies
        run: npm ci
        working-directory: backend

      - name: Run backend tests if available
        run: npm run test --if-present
        working-directory: backend

      - name: Upload backend artifact for deployment
        uses: actions/upload-artifact@v4
        with:
          name: node-app
          path: backend

  deploy:
    runs-on: ubuntu-latest
    needs: build
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Download backend artifact
        uses: actions/download-artifact@v4
        with:
          name: node-app

      - name: Login to Azure
        uses: azure/login@v2
        with:
         client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_DDF1F9FCA8374A14BA3BDA226A267D7A }}
         tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_D7CCCD1AE32E405E8D0D9D24A5826B8F }}
         subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_73C2064028B64C4EB1D564C21AB96351 }}

      - name: Deploy backend to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'campusfood-backend-bethuel'
          slot-name: 'Production'
          package: .
